#ifndef COMMANDS_H
#define COMMANDS_H

/* 
 *
 * returns 0 if input is OK, returns 1 if input is not OK
 */
struct result process_input(char _input[MAX_BUF]);

/*
 *
 */
//struct result calculate_rpn(struct token tokens[MAX_BUF]);

#endif
